{
  "name": "telemetry-export-four-domain",
  "description": "One runtime-derived value, four domains. A fabric exports telemetry from a pool of loopback addresses. The network leg harvests those addresses from the live switches and derives the smallest single range covering them. Three downstream legs — a Kubernetes namespace, a cloud archive bucket, and the observability stack that receives the telemetry — each authorise exactly that range, in three different configuration languages, none of which shares a vocabulary with the others.",
  "_whyThisPairing": "Every previous program crossed ONE domain boundary. This crosses three from a single producer, which is the property worth showing: the value is derived once, from state only a live harvest can know, and three independently-authored change packages must each consume it verbatim. A program that widened it anywhere, or recomputed it, or quietly used a different range in one leg, is wrong in a way no leg can detect on its own.",

  "fabric": {
    "devices": "the two switches reachable through the network read-only service",
    "exporterPool": {
      "purpose": "The loopback addresses from which the fabric exports telemetry.",
      "_whyThisIsUnarguablyRuntime": "The pool's members are individual host addresses configured per switch and advertised individually. They are asymmetric between the two devices, they are not contiguous, and the set is knowable only by reading both running configurations. Nothing outside a live harvest can enumerate them.",
      "addressesAreNotListedHere": "Discoverable ONLY by harvesting both devices. Do not assume, do not infer from a range name, and do not carry a value in from any other document.",
      "existenceAssumption": "The pool is already configured and already advertised as individual host routes. The change is to advertise the covering range in place of the individual routes — not to create the addresses."
    }
  },

  "cluster": {
    "namespace": "trading",
    "workload": "the namespace's running Deployment",
    "purpose": "The workload accepts telemetry from the fabric. Ingress to it must be restricted to the derived range and to nothing wider.",
    "existenceAssumption": "The namespace currently has NO NetworkPolicy. CREATE is the expected outcome, not modification. A default-deny posture plus the scoped allow is the shape; state what the policy must permit, not merely what it must deny."
  },

  "cloud": {
    "terraform_address": "aws_s3_bucket.app_logs",
    "workspace": "prod",
    "purpose": "The archive the fabric's telemetry is written to. Writes must be restricted to the derived range and to nothing wider.",
    "existenceAssumption": "The bucket exists; a bucket POLICY may not. CREATE of the policy is the expected outcome. The in-state captured secret on the neighbouring resource is never needed for this change and must never be read."
  },

  "observability": {
    "purpose": "The stack that receives the fabric's telemetry. It must alert when telemetry from the derived range stops arriving or is refused, and the alert must be scoped to that range rather than to all sources.",
    "existenceAssumption": "No rule scoped to the exporter sources exists today. CREATE is the expected outcome. The collector and the metrics stack are already running and already emit their own pipeline metrics — those are the observable quantities any threshold must be sized against.",
    "_whyThisLegIsNotDecoration": "This is the leg that closes the loop: the other three authorise a path, and this one is how anybody would learn the path had broken. A program that changes three systems and leaves no way to detect its own failure is incomplete, and a customer will ask about it."
  },

  "readOnlyServices": {
    "_whyThisIsHere": "Each leg reaches its rig ONLY through a self-provisioned read-only MCP service. This document is topology data and declares no endpoint, category or tools of its own, so the descriptors below are the connection surfaces and EACH must be named in its own leg's task description. Omitting one is not a soft failure: the harvest role is correct to refuse to invent connection fields, and that leg blocks at Phase 0.",
    "fabricDescriptorUrl": "https://raw.githubusercontent.com/paichart/paichart/main/descriptors/ceos-lab-readonly-descriptor.json",
    "clusterDescriptorUrl": "https://raw.githubusercontent.com/paichart/paichart/main/descriptors/k8s-readonly-descriptor.json",
    "cloudDescriptorUrl": "https://raw.githubusercontent.com/paichart/paichart/main/descriptors/terraform-readonly-descriptor.json",
    "observabilityDescriptorUrl": "https://raw.githubusercontent.com/paichart/paichart/main/descriptors/observability-readonly-descriptor.json",
    "lifecycle": "Self-provision per each leg's active protocol: register from the descriptor, harvest read-only, then tear down the registration — on approval AND on escalation alike."
  },

  "interdependency": {
    "direction": "fabric leg (UPSTREAM) → cluster, cloud and observability legs (three PARALLEL DOWNSTREAM consumers)",
    "crossingValue": "ONE range: the smallest single range covering every harvested exporter address across both devices.",
    "consumedVerbatim": "Each downstream leg consumes the derived range EXACTLY as published — never recomputed from its own view, never widened for convenience, never guessed. If it is absent from chained context, escalate rather than proceed.",
    "_whyParallelAndNotSequenced": "The three consumers do not depend on each other. Sequencing them would assert an ordering the change does not have, and a fabricated dependency is a defect in its own right: it would make a failure in one consumer block two unrelated ones. They share exactly one thing, and it comes from upstream.",
    "_whatThisTests": "One value crossing THREE domain boundaries at once, into three languages that share no vocabulary — a network routing policy, a Kubernetes NetworkPolicy, a cloud IAM condition, and a metrics alert scope. The only thing the four legs share is the number."
  }
}
