{
  "name": "@paichart/containment-checks",
  "version": "0.2.1",
  "description": "Mechanical checks for LLM-derived infrastructure values: did a derived CIDR/ASN actually respect the harvested ground truth? Pure functions — text in, fact out; a value the engine cannot check is reported as not-mechanically-covered, never as clean.",
  "license": "MIT",
  "author": "paichart",
  "repository": {
    "type": "git",
    "url": "https://github.com/paichart/paichart.git",
    "directory": "packages/containment-checks"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc",
    "test": "tsx test/derivation-containment.test.ts",
    "prepublishOnly": "npm run test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4",
    "typescript": "^5"
  },
  "keywords": [
    "llm", "agents", "verification", "containment", "cidr", "asn",
    "network-automation", "anti-fabrication", "derived-values", "mcp"
  ]
}
